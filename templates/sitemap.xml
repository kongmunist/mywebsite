<?xml version="1.0" encoding="utf-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <url>
    <loc>{{ url_for("page", path="", _external=True) }}</loc>
  </url>
  <br>
  <url>
    <loc>{{ url_for("page", path="blog", _external=True) }}</loc>
  </url>
  <br>
  <url>
    <loc>{{ url_for("page", path="projects", _external=True) }}</loc>
  </url>

  {% for post in posts %}
  <br>
    <url>
    <loc>{{ url_for("page", path=post.path, _external=True) }}</loc>
      {% for im in post.meta.images %}
        <image:image>
          <image:loc>
            {{ url_for("static", filename=im, _external=True) }}
          </image:loc>
        </image:image>
      {% endfor %}
    </url>
  {% endfor %}

</urlset>